{
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.7.0"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "npm run build && node dist/tests/runTests.js"
  }
}
